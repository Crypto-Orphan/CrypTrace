{
  "name": "cryptrace",
  "version": "1.0.0",
  "description": "Blockchain Explorer — Cloudflare Workers + Vanilla JS",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "tail":   "wrangler tail --format pretty"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
